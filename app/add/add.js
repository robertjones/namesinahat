// Generated by CoffeeScript 1.8.0
(function() {
  'use strict';
  angular.module('myApp.add', ['ngRoute']).config([
    '$routeProvider', function($routeProvider) {
      return $routeProvider.when('/add', {
        templateUrl: 'add/add.html',
        controller: 'AddCtrl'
      });
    }
  ]).controller('AddCtrl', [
    '$scope', '$cookieStore', '$location', function($scope, $cookieStore, $location) {
      var _ref, _ref1;
      $scope.names = (_ref = $cookieStore.get('names')) != null ? _ref : [];
      $scope.numTeams = (_ref1 = $cookieStore.get('numTeams')) != null ? _ref1 : 2;
      $scope.addName = function() {
        $scope.name.replace(/^\s+|\s+$/g, '');
        if ($scope.name !== '') {
          $scope.names.push($scope.name);
          $cookieStore.put('names', $scope.names);
          return $scope.name = '';
        }
      };
      $scope.empty = function() {
        if (confirm("Remove all names from the hat?")) {
          $scope.names = [];
          return $cookieStore.remove('names');
        }
      };
      return $scope.play = function() {
        $cookieStore.put('numTeams', parseInt($scope.numTeams));
        return $location.path("play");
      };
    }
  ]);

}).call(this);
